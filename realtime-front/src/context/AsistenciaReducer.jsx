const AsistenciaReducer = (state, action) => {
  switch (action.type) {
    case 'xxxxxxxxxxx':
      return {
        ...state,
      }
    default:
      return {
        ...state,
      }
  }
}

export default AsistenciaReducer
